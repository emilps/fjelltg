import React from 'react';
import Layout from '../components/Layout';

const NotFoundPage = () => (
  <Layout>
    <h1>PAGE NOT FOUND</h1>
    <div style={{ height: '400px' }}>
      You tried a page that doesn&#39;t exist...
    </div>
  </Layout>
);

export default NotFoundPage;
